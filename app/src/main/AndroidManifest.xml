<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.twistontime">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TwistOnTime">
        <activity
            android:name=".CustomizeNotificationScreen"
            android:label="CustomizeNotificationScreen"
            android:theme="@style/Theme.TwistOnTime.NoActionBar"></activity>
        <activity
            android:name=".CustomizeRepeatScreen"
            android:label="CustomizeRepeatScreen"
            android:theme="@style/Theme.TwistOnTime.NoActionBar"></activity>
        <activity
            android:name=".CustomizeSoundsScreen"
            android:label="CustomizeSoundsScreen"
            android:theme="@style/Theme.TwistOnTime.NoActionBar"></activity>
        <activity
            android:name=".PresetTimerScreen"
            android:label="@string/title_activity_preset_timer_screen"
            android:theme="@style/Theme.TwistOnTime.NoActionBar"></activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.TwistOnTime.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>